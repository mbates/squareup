# .github/dependabot.yml
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 10
    ignore:
      # typescript-eslint pins an upper bound on typescript (currently <6.1.0),
      # so a TS minor/major breaks `npm ci` until it catches up. Bump TS by
      # hand alongside typescript-eslint; patches still come through.
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major", "version-update:semver-minor"]
    groups:
      # `square` is the only peer dependency; its majors need a peer-range
      # decision, so keep them out of the tooling group.
      square:
        patterns:
          - "square"
      # Everything else is dev tooling. One group keeps packages that must
      # move in lockstep (vitest + @vitest/*, typescript-eslint + parser)
      # in the same PR.
      dev-dependencies:
        patterns:
          - "*"
        exclude-patterns:
          - "square"
    labels:
      - "dependencies"
    commit-message:
      prefix: "chore(deps):"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "chore(ci):"
